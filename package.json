{
  "name": "linkbase",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "pnpm --filter @linkbase/desktop dev",
    "build": "pnpm -r build",
    "tauri": "pnpm --filter @linkbase/desktop tauri"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
