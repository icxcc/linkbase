# 通用数据库客户端产品需求文档 (PRD) v1.1

**版本历史**

| 版本号  | 日期         | 作者   | 变更描述                |
| ---- | ---------- | ---- | ------------------- |
| v1.0 | 2026-04-15 | 产品团队 | 初稿，核心框架与功能          |
| v1.1 | 2026-04-29 | 产品团队 | 完善细节、非功能需求、插件系统、测试等 |

***

## 目录

1. [产品概述](#1-产品概述)\
   1.1 产品定位\
   1.2 产品目标\
   1.3 目标用户
2. [核心设计原则](#2-核心设计原则)
3. [功能需求](#3-功能需求)\
   3.1 连接管理\
   3.2 数据库对象浏览\
   3.3 SQL 编辑器\
   3.4 结果集展示与管理\
   3.5 数据导入/导出\
   3.6 数据可视化（插件）\
   3.7 模式比较与同步（插件）\
   3.8 用户工作区与偏好\
   3.9 新手引导与帮助
4. [非功能需求](#4-非功能需求)\
   4.1 性能\
   4.2 安全性\
   4.3 可用性\
   4.4 国际化与无障碍\
   4.5 兼容性\
   4.6 可维护性与可测试性
5. [技术架构](#5-技术架构)\
   5.1 整体架构\
   5.2 前端架构\
   5.3 后端架构（Rust crates）\
   5.4 前后端通信协议\
   5.5 连接与并发模型
6. [插件系统设计](#6-插件系统设计)\
   6.1 插件类型\
   6.2 插件清单规范\
   6.3 加载与生命周期\
   6.4 安全与隔离\
   6.5 依赖管理
7. [用户界面设计初稿](#7-用户界面设计初稿)\
   7.1 布局\
   7.2 核心交互流程\
   7.3 状态栏与上下文
8. [开发阶段与里程碑](#8-开发阶段与里程碑)
9. [风险与应对](#9-风险与应对)
10. [成功指标](#10-成功指标)
11. [测试策略](#11-测试策略)
12. [文档与交付物](#12-文档与交付物)
13. [附录](#13-附录)\
    A. 术语表\
    B. 竞品分析摘要\
    C. 错误码规范\
    D. 打包与分发策略初案

***

## 1. 产品概述

### 1.1 产品定位

为开发者、数据分析师和数据库管理员提供一款**跨平台、轻量级、高性能**的通用数据库管理工具。采用 **Tauri** 桌面框架，融合 Web 前端的灵活 UI 与 Rust 后端的高效与安全，实现多种关系型与非关系型数据库的统一管理。特别强调 **ODBC 通用接口** 和 **原生驱动插件** 的混合架构，满足不同场景下的连接需求。

### 1.2 产品目标

- 支持不少于 10 种主流数据库的稳定连接与操作。
- 提供原生般的启动速度（冷启动 ≤ 2 秒）和极低内存占用。
- 通过完善的插件系统实现数据库支持与功能的无限扩展，允许社区贡献驱动与工具。
- 构建可复用的组件库，使编辑器、结果集、导航等模块可独立迭代。
- 第一个可用版本（MVP）在 6 个月内交付。

### 1.3 目标用户

- **软件开发者**：日常开发中频繁查询、调试不同数据库，对编辑器智能提示要求高。
- **数据库管理员 (DBA)**：需要快速查看表结构、执行脚本、监控连接状态。
- **数据分析师**：习惯 SQL 查询，需要便捷的导入导出、简单可视化。
- **技术爱好者/学生**：需要免费、开源且可扩展的轻量级数据库工具。

***

## 2. 核心设计原则

- **模块化**：后端 Rust 核心拆分为独立 crate；前端 Vue 组件库按功能域拆分，避免循环依赖。
- **组件化**：UI 采用原子设计思想，从基础组件（按钮、输入框）到业务组件（SQL编辑器、虚拟表格），均可单独测试和替换。
- **插件化**：新数据库驱动、工具类功能（如数据可视化、代码生成器）均以插件形式动态加载，不侵入核心代码。
- **用户无感的数据源适配**：自动识别 ODBC 驱动与原生驱动，提供统一的连接配置界面，隐藏技术细节。
- **安全第一**：凭据加密存储，SQL 执行需用户确认，高危操作高亮警告，敏感信息自动脱敏。
- **可测试性**：从抽象层到 UI 均设计为可测试，确保稳定性。

***

## 3. 功能需求

### 3.1 连接管理

#### 3.1.1 连接类型与配置

- **ODBC 通用连接**：自动扫描系统已安装的 ODBC 驱动和数据源名称 (DSN)，用户可选择或直接输入连接字符串。支持 macOS (iODBC)、Linux (unixODBC) 和 Windows ODBC 驱动管理器。
- **原生驱动连接**：针对 MySQL、PostgreSQL、SQLite、SQL Server、MariaDB 等提供 Rust 原生驱动，以获得更佳性能、完整功能及方言特效。
- **连接字符串模式**：高级用户可直接输入 JDBC/ODBC 风格的连接字符串，并可在表单模式和字符串模式之间切换。
- **高级连接选项**：
  - **SSH 隧道**：支持密码和密钥认证，可配置本地端口转发。
  - **SSL/TLS**：支持证书验证、客户端证书、自定义 CA 等选项。
  - **HTTP 代理**（针对云数据库）。
  - **连接保活**：可配置心跳间隔，自动重连断开的连接。

#### 3.1.2 连接管理功能

- 创建、编辑、克隆、删除连接，支持文件夹分组管理。
- 连接测试与诊断：点击测试按钮显示延迟、驱动版本、服务器信息、SSL 状态等。
- 最近连接列表，支持快速重连和一键恢复。
- 导入/导出连接配置：加密导出（需输入主密码），明文导出时给予警告。
- 连接状态实时指示（已连接 / 断开 / 重连中）。

### 3.2 数据库对象浏览

- **树状导航**：连接 → 数据库/目录 → 模式 → 表、视图、物化视图、存储过程、函数、序列、同义词、触发器等。
- **对象快速搜索**：支持模糊匹配，跨模式搜索表名/列名。
- **右键上下文菜单**：
  - 生成 CRUD 语句 (SELECT/INSERT/UPDATE/DELETE)。
  - 复制对象名、DDL 预览。
  - 删除/截断（需二次确认及安全警告）。
  - 导出 DDL 到文件或剪贴板。
  - 脚本化数据 (导出 INSERT 语句)。
- **表详情面板**：
  - 列信息：名称、类型、可空、默认值、注释。
  - 索引、约束（主键、外键、唯一、检查）。
  - 触发器、规则（PG）。
  - DDL 预览（格式化显示，可复制）。
  - 表统计信息（行数、大小等，取决于数据库支持）。

### 3.3 SQL 编辑器

#### 3.3.1 编辑器核心

- **多标签页编辑器**：支持同时编辑多个查询，标签页可拖拽、拆分视图。
- **语法高亮与自动补全**：
  - 根据当前数据库方言提供智能补全（关键字、表名、列名、函数、代码片段）。
  - 上下文感知：跨模式引用时自动提示。
  - 支持表结构速览（补全列表内显示列信息）。
- **代码片段系统**：内置常用片段（如 `sel` → `SELECT * FROM`），用户可自定义库。
- **SQL 格式化**：美化/压缩 SQL，可配置缩进、大小写、逗号位置等风格。
- **多光标与列选择** (继承自 Monaco Editor)。
- **重构能力预留**：架构预留 Code Action 注册点，未来插件可提供“提取 CTE”等重构功能。

#### 3.3.2 执行与控制

- **执行选项**：执行全部、执行选中语句、逐条执行（以分号分隔）。
- **执行计划查看**：通过插件方式展示 `EXPLAIN` 结果（树形/图形）。
- **查询进度指示**：显示已执行时间、数据行数（动态更新），长查询可取消。
  - 后端提供 `cancel_query(connection_id, query_pid)` 接口。
  - 通过数据库原生机制取消（如 `pg_cancel_backend`, `KILL QUERY`）。
- **事务控制**：编辑器工具栏提供事务开始/提交/回滚按钮，当前事务状态清晰展示。

#### 3.3.3 查询历史与收藏

- 自动记录执行过的 SQL，支持搜索、过滤、回填到编辑器。
- 用户可固定查询到收藏夹，添加备注。

### 3.4 结果集展示与管理

#### 3.4.1 表格视图

- **虚拟滚动高性能表格**：支持百万行数据的流畅滚动（≥30fps），动态行高。
- **列操作**：排序、过滤（局部或服务端）、调整列宽、固定列、隐藏列。
- **数据拷贝**：复制单元格、行、列；可复制为 TSV/CSV/JSON/Markdown/INSERT 等多种格式。
- **记录编辑**：双击单元格进入编辑模式，修改后高亮变更行，提交时生成并执行 UPDATE 语句（事务模式下需手动提交）。
- **行号、NULL 值显式显示**。支持 BLOB/CLOB 查看器：十六进制预览、图片/文本预览，大对象延迟加载。

#### 3.4.2 多结果集处理

- 一次查询返回多个结果集时，以标签页或平铺方式展示。
- 支持结果集对比：分屏或并排显示两个结果，高亮差异。

#### 3.4.3 消息与日志

- 执行完成后显示受影响行数、执行时间。
- 错误信息友好化：数据库原始错误映射为用户可理解的消息，并提供“详细信息”折叠区显示原始堆栈（可配置）。
- 所有执行的 SQL 和结果摘要可查看日志面板。

### 3.5 数据导入/导出

#### 3.5.1 导出

- **格式支持**：CSV、JSON (数组/行对象可选)、Excel (.xlsx)、SQL INSERT 语句、Markdown。
- **导出配置**：编码、分隔符、包含标题行、NULL 值表示、日期时间格式等。
- **高级选项**：按查询结果或整表导出，可选择列子集，限制行数。
- **异步导出**：大数据集后台导出，进度条显示，可取消。

#### 3.5.2 导入

- **导入向导**：
  - 从 CSV、JSON、TSV 等文件导入。
  - 列映射：拖拽或选择源列→目标表列。
  - 数据预览与类型检测：自动推测列类型，允许手动调整。
  - 错误处理策略：忽略错误行、跳过并记录、终止导入。
  - 空值处理规则：指定空值标记。
- 导入过程显示进度，支持终止。

### 3.6 数据可视化（插件）

- 从结果集中一键生成基本图表（柱状图、折线图、饼图等）。
- 插件将使用图表库（如 ECharts），并提供配置面板选择维度与度量。
- MVP 阶段仅预留插件接口，后续版本正式提供。

### 3.7 模式比较与同步（插件）

- 比较两个数据库/表的结构差异，生成 ALTER 脚本。
- 计划作为独立插件，在 MVP 后进行开发。

### 3.8 用户工作区与偏好

- **布局保存**：面板位置、大小、显示/隐藏状态持久化。
- **主题**：内置亮色/暗色主题，支持自定义 CSS 变量（高级设置）。
- **编辑器设置**：字体族、字号、行高、制表符宽度、缩进方式。
- **快捷键**：提供可搜索的快捷键映射表，允许用户自定义。
- **全局配置**：文件编码、结果集每页大小、空值显示文字等。

### 3.9 新手引导与帮助

- **首次启动向导**：引导创建第一个连接（提供本地 SQLite 示例）。
- **示例数据库选项**：一键创建内存中的示例数据库，便于探索功能。
- **上下文帮助**：关键界面提供 tooltip，连接表单字段带解释。
- **帮助菜单**：链接到在线文档、插件开发指南、社区论坛。

***

## 4. 非功能需求

### 4.1 性能指标

- 应用冷启动时间 ≤ 2 秒。
- 打开 5 个数据库连接，10 个编辑器标签页，其中一个运行返回 10 万行数据的查询，内存占用 < 500 MB。
- 10 万行数据表格虚拟滚动帧率 ≥ 30fps。
- 编辑器打开 1 MB 的 SQL 文件无明显延迟 (< 500ms)。
- 查询取消响应时间 < 2 秒。

### 4.2 安全性

- **凭据保护**：
  - 密码使用操作系统密钥链 (Windows Credential Manager, macOS Keychain, Linux Secret Service) 或 AES-256 加密文件存储。
  - 主密码保护：可选，用于加密本地配置文件。
  - 内存中密码使用后立即清零。
- **日志脱敏**：连接字符串和 SQL 日志自动过滤密码、密钥等敏感信息。
- **高危操作确认**：无 WHERE 的 UPDATE/DELETE、DROP、TRUNCATE 等执行前弹出警示确认，并显示影响范围（若可获取）。
- **插件安全模型** (详见 6.4)。

### 4.3 可用性

- 遵循各平台 (Windows, macOS, Linux) 人机界面指南，支持系统菜单、托盘图标。
- 完全键盘可操作，支持常见快捷键和焦点指示器，符合 WCAG 2.1 AA。
- 错误信息和提示语使用用户语言，技术细节折叠显示。
- 界面元素支持高分屏 (HiDPI) 缩放。

### 4.4 国际化与无障碍

- **国际化 (i18n)**：采用 `vue-i18n` 管理所有可见文本，首次发布至少支持简体中文、英文。
- 插件强制要求遵循 i18n 规范，否则不允许上架市场。
- **无障碍 (A11y)**：在里程碑中加入专项测试，检查颜色对比度、屏幕阅读器兼容性、键盘导航逻辑。

### 4.5 兼容性

- **操作系统**：Windows 10+ (x64, arm64 可选)，macOS 11+ (Intel, Apple Silicon)，Ubuntu 20.04+ (及其他主流 Linux 发行版)。
- **数据库支持列表**：
  - MVP 阶段原生驱动：SQLite, MySQL 5.7+/8.0, PostgreSQL 10+, MariaDB 10+, SQL Server 2016+。
  - MVP 阶段 ODBC 适配：Oracle, DB2, 及其他任何 ODBC 兼容数据库。
  - 未来扩展：ClickHouse, MongoDB, Redis, Cassandra 等。

### 4.6 可维护性与可测试性

- **架构约束**：
  - 前端：组件不得直接调用 Tauri `invoke`，必须通过 Pinia Store 或可组合函数封装。
  - 后端：数据库驱动不得直接操作文件系统或网络，必须通过核心抽象接口。
  - 所有公共接口需编写文档注释。
- **代码规范**：Rust 使用 `clippy` 严格模式，前端使用 ESLint + Prettier。
- **测试要求**：详见第 11 节。

***

## 5. 技术架构

### 5.1 整体架构图

```
┌────────────────────┐      ┌─────────────────────────┐
│   Vue 3 前端       │◄────►│   Tauri (Rust 后端)      │
│  - TypeScript      │ IPC  │  - 连接与查询管理         │
│  - Pinia Store     │      │  - SQL 执行引擎           │
│  - Tailwindcss     │
│  - Monaco Editor   │      │  - 驱动抽象层 (Trait)     │
│  - 自研虚拟表格    │      │  - 插件加载器             │
│  - 主题/i18n       │      │  - 加密与安全             │
└────────────────────┘      └───────────┬─────────────┘
                                        │
                ┌───────────────────────┼───────────────────────┐
                │                       │                       │
        ┌───────▼────────┐   ┌─────────▼──────────┐   ┌───────▼────────┐
        │ ODBC 适配器    │   │ MySQL/Postgres/...  │   │ 自定义驱动插件  │
        │ (odbc-api)     │   │ 原生驱动 (sqlx 等)  │   │ (.so/.dll)     │
        └────────────────┘   └────────────────────┘   └────────────────┘
```

### 5.2 前端架构

- **框架**：Vue 3 (Composition API) + TypeScript。
- **构建工具**：Vite，采用 pnpm monorepo 管理多包。
- **状态管理**：Pinia，按功能域拆分 store (connection, editor, result, schema…)。
- **UI 库**：整体UI风格扁平简约，基于 Tailwindcss，封装为 `@app/components`，保证风格统一且可替换。
- **编辑器**：Monaco Editor `@monaco-editor/loader`，按需加载语言支持。
- **虚拟表格**：自研基于 `@tanstack/virtual` 的表格组件 `@app/result-grid`，支持动态行高、列调整。
- **包结构 (monorepo)**：
  - `@app/core`：应用壳、主题、路由、i18n 等。
  - `@app/connection`：连接管理 UI。
  - `@app/editor`：SQL 编辑器及辅助功能。
  - `@app/result`：结果集展示和导出 UI。
  - `@app/schema`：对象浏览器。
  - `@app/data-grid`：可编辑数据视图。
  - `@app/plugins`：插件管理界面。
  - `@app/components`：通用组件库。

### 5.3 后端架构（Rust crates）

- `core`：连接池、会话管理、配置读写、查询调度。
- `db-common`：驱动抽象 trait 定义 (`DbDriver`, `QueryResult`, `Metadata`, `CancellableQuery` 等)。
- `drivers`：
  - `odbc-driver`：基于 `odbc-api` 库实现 ODBC 对接，统一处理 unixODBC/iODBC/Windows ODBC 差异。
  - `mysql-driver`：基于 `sqlx` (或 `mysql_async`)。
  - `postgres-driver`：基于 `sqlx` (或 `tokio-postgres`)。
  - `sqlite-driver`：基于 `rusqlite`。
  - `mssql-driver`：基于 `tiberius`。
- `plugin-system`：使用 `libloading` 动态加载，解析 manifest，管理生命周期。
- `security`：集成 `keyring` crate 和 `aes-gcm`。
- `exporter`：实现各种格式导出，支持流式写入。
- `telemetry`：可选匿名使用统计和崩溃报告（基于 `sentry` 或自定义）。

### 5.4 前后端通信协议

- 使用 Tauri 命令 (`tauri::command`) 暴露功能，前端通过 `invoke` 调用。
- 命令参数和返回值使用 JSON 序列化，关键结构定义使用 `serde` 并生成 TypeScript 类型（通过 `ts-rs` 或手动同步）。
- 异步查询结果通过 Tauri 事件机制推送（进度、部分结果集、取消通知）。
- 定义统一的错误响应格式：
  ```json
  {
    "error": {
      "code": "ERR_DB_CONNECTION_TIMEOUT",
      "message": "无法连接到数据库：连接超时",
      "detail": "...",
      "suggestion": "请检查主机地址和端口，或网络连接"
    }
  }
  ```

### 5.5 连接与并发模型

- 每个数据库连接在 Rust 后端映射为一个连接池（或单连接）对象，由 `core` 管理器持有。
- 前端请求发送到 Tauri 命令，后台异步执行 SQL，通过 `tokio` 任务管理。
- 同一连接上的查询默认串行执行（可通过配置允许并发），避免事务冲突。
- 查询取消通过发送 `CancellationToken` 通知后台任务，再由驱动层执行对应的数据库取消操作。
- 连接池参数可配置（最小/最大连接数、空闲超时等）。

***

## 6. 插件系统设计

### 6.1 插件类型

- **数据库驱动插件**：提供新数据库支持，可以是 ODBC 扩展或原生实现。
- **工具插件**：扩展功能，如数据可视化、模式比较、代码生成器等。
- **编辑器扩展插件**：向 Monaco 添加补全提供器、悬停提示、代码操作等。
- **主题插件**：提供新的颜色主题。
- **数据导出插件**：新增导出格式。

### 6.2 插件清单规范 (plugin.toml)

```toml
[plugin]
name = "Oracle ODBC Driver"
version = "1.0.0"
author = "community"
description = "Oracle 数据库 ODBC 驱动支持"
plugin_type = "database_driver"   # database_driver, tool, editor_extension, theme, exporter
entry_point = "liboracle_plugin"  # 动态库文件名 (不带扩展名)
backend_language = "rust"
frontend_entry = "frontend/index.js" # 如果涉及前端代码，入口文件

[dependencies]
foundation = ">=0.1.0"

[driver]
display_name = "Oracle (via ODBC)"
protocol = "odbc"
odbc_driver_names = ["Oracle in instantclient", "Oracle ODBC driver"]

[permissions]
network = true
filesystem = ["$TEMP"]
sandbox = false  # 若为 false 则运行在子进程中

[frontend_hooks]
"result:contextmenu" = "add_export_oracle"
"editor:autocomplete" = "oracle_keywords"
```

### 6.3 加载与生命周期

1. 用户通过“插件市场”或本地安装 `.plugin` 包（实质为 tar.gz）。
2. 插件管理器解压到 `{data_dir}/plugins/{name}/`，验证数字签名（如内置签名校验）。
3. 读取 manifest，若需要启动子进程则 spawn 并建立 IPC。
4. 动态库加载：后端通过 `libloading` 加载并调用 `register_plugin` 传递宿主接口指针。
5. 前端通过 Vue 的 `defineAsyncComponent` 加载前端入口模块，并注册到钩子系统。
6. 卸载时调用 `unregister`，释放资源。

### 6.4 安全与隔离

- **信任级别**：
  - **核心内置**：完全信任，与主程序一起编译。
  - **已验证签名**：通过官方或认可渠道签名，允许网络和有限文件系统访问，运行在进程内但受限轻沙箱。
  - **未签名/社区**：强制运行在独立子进程中，无网络权限，文件访问仅限于临时目录，进程崩溃不影响主程序。
- 权限声明在 manifest 的 `[permissions]` 中，不声明则默认最小权限。
- 所有插件调用宿主 API 均经过权限检查。

### 6.5 依赖管理

- 插件 manifest 中 `[dependencies]` 指定依赖的其他插件及版本。
- 管理器在加载前进行拓扑排序，确保顺序正确。
- 版本冲突时提示用户，不允许加载。

***

## 7. 用户界面设计初稿

### 7.1 主布局

- **标题栏**：左侧应用名称、菜单（自定义或系统集成）？，右侧 设置 最大化、最小化、关闭。
- **工具栏**：连接选择器、SQL 执行按钮、事务控件、主题切换快捷。
- **左面板**：连接与数据库对象浏览器。
- **右面板（主区域）**：
  - **上部**：多标签页 SQL 编辑器。
  - **下部**：可拖拽高度的结果面板（表格/原始数据/消息）。
- **底部状态栏**：当前连接信息、数据库类型、SQL 语法、光标位置、查询执行时间/行数。
- **命令面板**：Ctrl+Shift+P 唤起，搜索并执行所有功能。

### 7.2 核心交互流程

- **新建连接**：工具栏点击“+”→ 选择数据库类型（或扫描 ODBC 驱动）→ 填写参数 → 测试 → 保存并连接。
- **执行查询**：在编辑器中编写 SQL → 点执行或快捷键 → 结果在下方面板展示。
- **数据浏览**：在对象树双击表名 → 自动生成 `SELECT * LIMIT 100` 并运行，结果以可编辑网格展示。

### 7.3 状态栏内容

- 左区：当前活跃连接名 / 数据库类型图标。
- 中区：最新执行的 SQL 片段或状态（就绪/执行中/已取消）。
- 右区：执行时间、返回行数。

***

## 8. 开发阶段与里程碑 (Roadmap)

| 阶段  | 时间       | 内容                                                               | 交付物                  |
| --- | -------- | ---------------------------------------------------------------- | -------------------- |
| M0  | 第 1-2 月  | 基础架构：Tauri + Vue 脚手架，monorepo，核心布局、主题、路由。Rust 驱动抽象与 SQLite 驱动实现。 | 可运行的壳，SQLite 基本连接与查询 |
| M1  | 第 3-4 月  | 连接管理全功能，MySQL/PostgreSQL 驱动，SQL 编辑器（Monaco 集成与补全），结果表格虚拟滚动，查询历史。 | 核心工作闭环，可日常使用         |
| M2  | 第 5 月    | ODBC 适配器，SQL Server 支持，插件加载器原型，数据导入导出向导，SSH/SSL 隧道。              | 扩展能力演示，插件可安装         |
| M3  | 第 6 月    | 完整测试、i18n 中英文、安装包与自动更新、文档站点。发布 v0.1.0。                           | 正式 MVP 发布            |
| M4+ | 第 7 月及以后 | 插件市场、数据可视化、模式比较、NoSQL 扩展、性能优化。                                   | 持续迭代                 |

注：每个阶段末尾安排一次 **内部演示与可用性测试**，并修复 P0/P1 bug。

***

## 9. 风险与应对

| 风险                   | 概率 | 影响 | 缓解措施                                              |
| -------------------- | -- | -- | ------------------------------------------------- |
| ODBC 跨平台驱动兼容性问题      | 高  | 高  | 专项测试主流 ODBC 驱动，提供各平台配置文档；与 unixODBC/iODBC 社区保持沟通。 |
| Monaco Editor 打包体积过大 | 中  | 中  | 按需加载语言服务，提供轻量级 Fallback 编辑器（如 CodeMirror）作为可选。    |
| Rust 动态库插件沙箱实现复杂     | 中  | 中  | MVP 仅允许内置和已验证签名插件，子进程隔离在 M4+ 实现。                  |
| 不同数据库方言元数据查询差异大      | 中  | 高  | 抽象层设计通用元数据接口，方言特化通过策略模式实现，编写详尽的集成测试。              |
| 应用性能在大量连接/查询下退化      | 低  | 高  | 早期加入性能基准测试，使用连接池与异步模型，结果集使用流式处理。                  |
| 社区贡献插件质量参差不齐         | 中  | 中  | 建立插件审核流程，签名机制，安全沙箱，用户评级系统。                        |

***

## 10. 成功指标

- 发布 3 个月后 GitHub Stars ≥ 2000。
- 活跃安装量 ≥ 5000（通过自动更新匿名统计）。
- 社区贡献 ≥ 5 个第三方数据库驱动插件（或工具插件）。
- 平均崩溃率 ＜ 0.5% 会话。
- 应用在主要操作系统上的冷启动时间中位数 ≤ 1.8 秒。

***

## 11. 测试策略

| 测试层级       | 范围              | 工具/方法                                         | 覆盖目标        |
| ---------- | --------------- | --------------------------------------------- | ----------- |
| **单元测试**   | Rust 抽象层、工具函数   | `cargo test`，覆盖率 ≥ 80%                        | 核心逻辑        |
| **集成测试**   | 数据库驱动、API 命令    | 使用 `testcontainers` 启动真实数据库实例，测试连接、查询、取消等核心场景 | 所有驱动        |
| **组件测试**   | Vue 组件          | Vitest + Vue Test Utils，关键交互路径                | 前端组件        |
| **E2E 测试** | 完整用户工作流         | Tauri WebDriver (或 Spectron 替代)               | 关键场景 ≥ 90%  |
| **性能测试**   | 大数据量表格、大文件编辑    | 编写 Benchmark 用例，纳入 CI                         | 达标指标        |
| **安全测试**   | 密码存储、日志脱敏、权限    | 人工 Review + 自动化静态分析 (cargo-audit, npm audit)  | 关键风险点       |
| **无障碍测试**  | 颜色对比度、键盘导航、屏幕阅读 | axe-core, 手动检查清单                              | WCAG 2.1 AA |

CI/CD 流水线上必须通过单元、集成、组件测试及 lint 检查，E2E 测试夜间执行。

***

## 12. 文档与交付物

- **产品文档**：用户手册 (中文/英文)，包含快速入门、功能介绍、常见问题。
- **开发者文档**：架构说明、插件开发指南、API 参考。
- **构建与打包**：各平台安装包（.msi, .dmg, .AppImage/.deb），自动更新服务器配置。
- **官网与社区**：Landing page，GitHub Discussions，插件市场目录。

***

## 13. 附录

### 附录 A：术语表

- **Tauri 命令**：Rust 后端暴露给前端的函数，通过 IPC 调用。
- **驱动抽象层 (Trait)**：定义 `Connect`, `Query`, `GetMetadata` 等方法的 Rust trait，所有数据库驱动实现它。
- **插件**：可动态加载的扩展，可包含后端动态库和前端代码块。
- **虚拟滚动**：仅渲染可见行以提高性能的表格技术。
- **ODBC**：开放数据库互连，允许应用程序通过标准接口访问不同 DBMS。

### 附录 B：竞品分析摘要

| 竞品       | 优势            | 劣势                     | 本产品差异化         |
| -------- | ------------- | ---------------------- | -------------- |
| DBeaver  | 数据库支持极广，功能丰富  | 基于 Eclipse，启动慢，内存占用高   | 轻量、启动快，现代 UI   |
| Navicat  | 界面友好，稳定       | 商业收费，不支持插件扩展           | 免费开源，高度可扩展插件系统 |
| DataGrip | 智能提示强大，IDE 集成 | 付费订阅，资源消耗大             | 完全免费，专注于通用客户端  |
| HeidiSQL | 轻量，Windows 友好 | 仅 Windows，功能有限，不支持云数据库 | 跨平台，现代化技术栈     |

### 附录 C：错误码规范

错误码格式：`分类_具体错误`，分类包括 `ERR_DB` (数据库), `ERR_CONN` (连接), `ERR_QUERY` (查询), `ERR_IO` (文件), `ERR_PLUGIN` (插件) 等。每个错误码附带 `message`, `detail`, `suggestion` 字段，支持国际化。

### 附录 D：打包与分发策略初案

- 使用 `tauri build` 生成各平台安装包。
- 自动更新依赖 Tauri updater，通过 CDN 分发签名后的更新包。
- 插件市场以静态 JSON 索引方式托管，客户端定期拉取，支持 HTTP 下载安装。
- 代码签名：Windows 使用 EV Code Signing，macOS 必须公证。

***

**文档状态**：已完善，可进入评审与原型设计阶段。
